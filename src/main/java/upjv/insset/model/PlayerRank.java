package upjv.insset.model;

/**
 * Rangs disponibles dans le jeu Tuuuur (Culture Générale Compétitif).
 * Chaque rang débloque des articles de merch exclusifs dans la boutique.
 */
public enum PlayerRank {
    BRONZE,
    ARGENT,
    OR,
    PLATINE,
    DIAMANT,
    CHALLENGER
}
